srcs=Split("""
framebufferdrv.c
hdrdrv.c
openexrdrv.c
sockdrv.c
""")

#jpeg.c

Import('env')

incPath=['../base', '../ri', '../imageio', '../../include']
libName='ridisplay'

env.Library(libName, srcs, CPPPATH=incPath)
