# ---------------------------------------------------------------------------
# Compile options. choose the one as you like ;)
#

# AMD 64bit with optimization including SSE auto vectorization
CFLAGS = -DLINUX -DNDEBUG -fast -xvector -xarch=amd64a

# Generic, SSE disaable
#CFLAGS = -DLINUX -DNDEBUG -O

#
#
# ---------------------------------------------------------------------------
